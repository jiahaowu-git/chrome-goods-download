---
phase: "01"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - background.js
  - content.js
  - sidepanel.html
autonomous: true
requirements:
  - SHELL-03
  - SHELL-04

must_haves:
  truths:
    - "Service worker starts on extension load"
    - "Content script can send messages to service worker"
    - "Service worker can relay messages to side panel"
    - "Side panel receives data from content script"
  artifacts:
    - path: "background.js"
      provides: "Background service worker with message routing"
      contains: "chrome.runtime.onInstalled"
    - path: "content.js"
      provides: "Content script with message listener"
      contains: "chrome.runtime.onMessage"
    - path: "sidepanel.html"
      provides: "Side panel entry point"
  key_links:
    - from: "content.js"
      to: "background.js"
      via: "chrome.runtime.sendMessage"
    - from: "background.js"
      to: "sidepanel.html"
      via: "chrome.runtime.sendMessage to tab"
---

<objective>
Enhance background.js service worker and content.js to establish proper bidirectional messaging per SHELL-03 and SHELL-04. Update sidepanel.html to serve as proper UI entry point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@background.js
@content.js
@sidepanel.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance background.js service worker</name>
  <files>background.js</files>
  <read_first>
    - background.js
  </read_first>
  <action>
    Read existing background.js and enhance it with proper service worker functionality for SHELL-03:

    Required functionality:
    1. chrome.runtime.onInstalled listener (already exists) - keep it
    2. chrome.sidePanel.setPanelBehavior with openPanelOnActionClick: true (already exists) - keep it
    3. Add chrome.runtime.onMessage listener to receive messages from content script
    4. When message received from content script, forward it to the active tab's side panel via chrome.runtime.sendMessage
    5. Handle error cases - if no active tab, log warning

    Message routing pattern:
    - content.js sends {type: 'pageData', data: {...}} to background
    - background receives and forwards to sidepanel.html in active tab
  </action>
  <verify>
    <automated>grep -c "chrome.runtime.onMessage" background.js && grep -c "chrome.runtime.sendMessage" background.js</automated>
  </verify>
  <done>background.js has onMessage listener and sendMessage forwarding to tab</done>
</task>

<task type="auto">
  <name>Task 2: Enhance content.js with extraction-ready messaging</name>
  <files>content.js</files>
  <read_first>
    - content.js
  </read_first>
  <action>
    Read existing content.js and enhance it with proper content script functionality for SHELL-04:

    Required functionality:
    1. Retain existing chrome.runtime.onMessage listener (already exists) - keep it
    2. Add a function to extract page data (placeholder for now - actual extraction comes in Phase 2)
    3. On document_idle, optionally auto-send a 'ready' message to background
    4. Add message types: 'getPageData' request, 'pageData' response
    5. Use chrome.runtime.sendMessage to send data to background (not sendResponse pattern for one-way)

    The content script should be extraction-ready (actual extraction in Phase 2) but have the messaging infrastructure in place.
  </action>
  <verify>
    <automated>grep -c "chrome.runtime.sendMessage" content.js</automated>
  </verify>
  <done>content.js has sendMessage capability for sending page data to background</done>
</task>

<task type="auto">
  <name>Task 3: Update sidepanel.html structure</name>
  <files>sidepanel.html</files>
  <read_first>
    - sidepanel.html
  </read_first>
  <action>
    Read existing sidepanel.html and enhance it with proper structure for Phase 3 UI:

    Current state: Has basic dark theme styling already
    Required updates:
    1. Ensure body has id="sidepanel-body" for JavaScript targeting
    2. Add div id="app" as mount point for React/UI (Phase 3 will add actual UI)
    3. Add script tag for a minimal sidepanel.js (we will create this in Phase 3, but structure should exist)
    4. Keep the dark theme styles (#1a1a1a background, #f5f5f5 text) - these match UI-02 requirements
    5. Add loading state div with id="loading" that shows initially

    The sidepanel.html should be a proper shell that Phase 3 UI components can build upon.
  </action>
  <verify>
    <automated>grep -c 'id="app"' sidepanel.html && grep -c 'id="loading"' sidepanel.html</automated>
  </verify>
  <done>sidepanel.html has app mount point, loading element, and dark theme styles in place</done>
</task>

</tasks>

<verification>
1. Open chrome://extensions/
2. Reload the extension
3. Click toolbar icon to open side panel
4. Open a Taobao/Tmall page (or any page matching content_scripts matches)
5. Check console for content script log: "chrome-goods-download content script injected"
6. Check background console for service worker logs
</verification>

<success_criteria>
- background.js starts without errors in chrome://extensions/
- Content script injects on matching pages
- Messaging infrastructure is in place for Phase 2 integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-shell/01-02-SUMMARY.md`
</output>
