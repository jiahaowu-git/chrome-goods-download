---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02

must_haves:
  truths:
    - "Extension appears in chrome://extensions/ without errors"
    - "Toolbar icon displays at all sizes"
    - "Side panel path is correctly configured"
  artifacts:
    - path: "manifest.json"
      provides: "Extension configuration with Side Panel API"
      contains: "sidePanel"
    - path: "icons/icon16.png"
      provides: "16px toolbar icon"
    - path: "icons/icon32.png"
      provides: "32px toolbar icon"
    - path: "icons/icon48.png"
      provides: "48px toolbar icon"
    - path: "icons/icon128.png"
      provides: "128px icon"
  key_links:
    - from: "manifest.json"
      to: "sidepanel.html"
      via: "side_panel.default_path"
    - from: "manifest.json"
      to: "background.js"
      via: "background.service_worker"
    - from: "manifest.json"
      to: "content.js"
      via: "content_scripts.js"
---

<objective>
Verify and finalize manifest.json and icon files for Phase 1 extension shell. Ensure all permissions, paths, and icons are correctly configured per SHELL-01 and SHELL-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@manifest.json
@icons/icon16.png
@icons/icon32.png
@icons/icon48.png
@icons/icon128.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify manifest.json conformance</name>
  <files>manifest.json</files>
  <read_first>
    - manifest.json
  </read_first>
  <action>
    Read the existing manifest.json and verify it contains all required fields:

    Required fields per SHELL-01:
    1. permissions array must include "sidePanel"
    2. side_panel.default_path must be "sidepanel.html"
    3. background.service_worker must be "background.js"
    4. content_scripts array must include entry with js:"content.js" and matches for taobao/tmall
    5. action.default_icon must define 16, 32, 48
    6. icons must define 16, 32, 48, 128

    If any field is missing, add it. If values are incorrect, fix them.
  </action>
  <verify>
    <automated>python -c "import json; m=json.load(open('manifest.json')); assert 'sidePanel' in m['permissions'], 'missing sidePanel permission'; assert m['side_panel']['default_path']=='sidepanel.html', 'wrong side_panel path'; print('manifest.json valid')"</automated>
  </verify>
  <done>manifest.json has all SHELL-01 fields: sidePanel permission, side_panel path, background service worker, content_scripts config, and all icon definitions</done>
</task>

<task type="auto">
  <name>Task 2: Verify icon files exist and are valid PNGs</name>
  <files>
    - icons/icon16.png
    - icons/icon32.png
    - icons/icon48.png
    - icons/icon128.png
  </files>
  <read_first>
    - icons/icon16.png
    - icons/icon32.png
    - icons/icon48.png
    - icons/icon128.png
  </read_first>
  <action>
    Verify each icon file exists and is a valid PNG image by checking file headers.

    Required icons per SHELL-02:
    - icons/icon16.png (16x16 pixels)
    - icons/icon32.png (32x32 pixels)
    - icons/icon48.png (48x48 pixels)
    - icons/icon128.png (128x128 pixels)

    Check each file:
    1. File exists
    2. File size > 0
    3. PNG magic bytes (first 8 bytes: 89 50 4E 47 0D 0A 1A 0A)

    If any icon is missing or corrupt, log which one.
  </action>
  <verify>
    <automated>python -c "import struct; files=['icons/icon16.png','icons/icon32.png','icons/icon48.png','icons/icon128.png']; errors=[]; [errors.append(f+': missing') if not __import__('os').path.exists(f) else None for f in files]; png=bytes([137,80,78,71,13,10,26,10]); [errors.append(f+': bad header') if list(__import__('os').path.getsize(f)>0 and open(f,'rb').read(8)!=png) for f in files]; print('OK' if not errors else ', '.join(errors))"</automated>
  </verify>
  <done>All 4 icon files exist and are valid PNGs with correct magic bytes</done>
</task>

</tasks>

<verification>
1. Open chrome://extensions/
2. Enable Developer mode
3. Load unpacked extension from project directory
4. Verify no errors shown for this extension
5. Verify icon appears in toolbar
</verification>

<success_criteria>
- manifest.json passes validation check
- All 4 icon files exist as valid PNGs
- Extension loads without errors in chrome://extensions/
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-shell/01-01-SUMMARY.md`
</output>
